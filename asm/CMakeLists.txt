file(
  GLOB_RECURSE
  GENERATED_ASM_SOURCES
  CONFIGURE_DEPENDS
  ${CMAKE_CURRENT_SOURCE_DIR}/*.s)

list(
  FILTER
  GENERATED_ASM_SOURCES
  EXCLUDE
  REGEX
  "^${CMAKE_CURRENT_SOURCE_DIR}/nonmatchings/.*\\.s$")

target_sources(${TARGET} PRIVATE ${GENERATED_ASM_SOURCES})
