/**
 * @file demon_dracula_assets_loader.c
 *
 * `DemonDraculaAssetsLoader`'s only purpose is to load
 * the assets file for Demon Dracula (via its `objects_file_info` entry)
 * To do that, the object itself doesn't have to run any code,
 * therefore it is empty.
 */

#include "objects/enemy/demon_dracula/demon_dracula_assets_loader.h"

void DemonDraculaAssetsLoader_entrypoint(DemonDraculaAssetsLoader* self) {}
